apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.externalSecrets.env.name }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: {{ .Release.Name }}-aws-secretsmanager
    kind: SecretStore
  target:
    name: {{ .Values.externalSecrets.env.targetSecretName }}
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: {{ .Values.externalSecrets.env.awsSecretName }}
